railsFortune

It's simple rails3 application which displays famous quotes.

1. Configuration
In order to get it running you need to register app on github and provide secret key in config/initializers/omniauth.rb

2. Installation/Execution
bundle install
rake db:migrate
rails s

To load sample fortunes execute this command 
rake db:fixtures:load
