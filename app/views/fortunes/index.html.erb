<h1>Listing fortunes</h1>
<div id="listMenu">
	<%= will_paginate @fortunes %>
</div>
<div class="grid">
	<% @fortunes.each do |fortune| %>
	<div class="gridElement">
		<%= render :partial => "fortune_big", :locals  => { :fortune => fortune }  %>
	</div>
	<% end %>

	<br />
</div>
