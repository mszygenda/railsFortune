var partial = "<%= escape_javascript render :partial => "comment", :locals => { :comment => @comment } %>";
var commentsDiv = $("#comments_<%= @comment.fortune_id %> > div.commentsList");
commentsDiv.prepend(partial);
var commentDiv = $("div.comment:first", commentsDiv);
commentDiv.hide();
commentDiv.fadeIn();
